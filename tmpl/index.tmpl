    {{ define "Index" }}
       {{ template "Header" }}
          {{ template "Menu" }}

<h2>Registered users: {{.Count}}</h2>


<div>
    <table border="1">
        <thead>
            <tr>
                <td>Id</td>
                <td>Name</td>
                <td>Email</td>
                 <td>Salary</td>
               <td>Action</td>
            </tr>
        </thead>

        <tbody>



            <tr key="index">


                <td>{{range .Employee}} {{ .Id }}</br>{{end}}</td>
                <td>{{range .Employee}} {{ .Name }}</br>{{end}}</td>
                <td>{{range .Employee}} {{ .Email }}</br>{{end}}</td>
                <td>{{range .Employee}} {{ .Salary }}</br>{{end}}</td>

                <td> {{range .Employee}} <a href="/show?id={{.Id}}">View</a> | <a href="/edit?id={{.Id}}">Edit</a></br> {{end}} </td>
            </tr>

        </tbody>
    </table>
    </div>
    <br/>
 {{ end }}
    <a href="/csv"  download=go.csv> DOWNLOAD CSV </a>

 {{template "Footer"}}
